const express = require("express");
const router = express.Router();

router.get("/", (req, res) => {
    res.send("AI Model");
});

router.get("/", (req, res) => {
    res.send("AI Model");
});
router.get("/", (req, res) => {
    res.send("AI Model");
});

module.exports = router;